{
  "type": "function",
  "name": "generate_rationale",
  "description": "生成下一步要使用 ask_end_of_session、clarify_book_request、search_library_catelog 哪一個 tool 的推理，依據 prepare_environment_snapshot 與 prepare_inferred_context_snapshot 回傳值和與使用者的對話紀錄等資訊，詳細說明為何選擇這個 tool。",
  "strict": true,
  "parameters": {
    "type": "object",
    "properties": {
      "rationale": {
        "type": "string",
        "description": "推理的內容。"
      }
    },
    "required": ["rationale"],
    "additionalProperties": false
  }
}